# Agent Dreams

Nova fleet memory consolidation diary (REM + Deep). **Not a promotion source** — only grounded pending facts from agent memories may promote into `.cursor/agents/*.md`.

Run: `py -3 tools/agent_dream.py` (dry-run) or `py -3 tools/agent_dream.py --write`.

See [[Agent-Dreaming]] in the Obsidian vault (`docs/agent-operations.md`).

> **Public repo note:** Dream/quota diagnostics are session-local. This file intentionally omits provider error dumps and per-run promotion details from private runs.

## REM

_(No archived REM entries in the public snapshot.)_

## Deep Sleep

_(No archived Deep Sleep entries in the public snapshot.)_
